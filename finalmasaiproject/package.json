{
  "name": "shoplocal",
  "version": "1.0.0",
  "description": "ShopLocal - E-Commerce Platform",
  "scripts": {
    "dev": "concurrently -k -n Backend,Frontend -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "concurrently -k -n Backend,Frontend -c cyan,magenta \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
